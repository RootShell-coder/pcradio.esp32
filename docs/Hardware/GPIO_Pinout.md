# Карта GPIO PCRadio

Актуально для firmware `2.2.6`, ESP32-S3 N16R8:

## Полная таблица

| GPIO   | Имя в firmware            | Направление / периферия   | Активный уровень и назначение                      |
| ------ | ------------------------- | ------------------------- | -------------------------------------------------- |
| GPIO1  | `GPIO_IR_RX`              | вход, RMT RX              | импульсный вход ИК-приёмника                       |
| GPIO2  | `GPIO_STATUS_WLED`        | выход, RMT TX             | данные для цепочки из пяти адресных RGB LED        |
| GPIO4  | `GPIO_BUTTON_PREV`        | вход с внутренним pull-up | LOW: предыдущая станция                            |
| GPIO5  | `GPIO_BUTTON_PLAY`        | вход с внутренним pull-up | LOW: Play/Stop                                     |
| GPIO6  | `GPIO_BUTTON_NEXT`        | вход с внутренним pull-up | LOW: следующая станция                             |
| GPIO7  | `GPIO_BUTTON_MUTE`        | вход с внутренним pull-up | LOW: Mute                                          |
| GPIO8  | `GPIO_AMP_POWER`          | цифровой выход            | HIGH: включить питание усилителя                   |
| GPIO9  | `I2S_MUTE_PIN`            | цифровой выход            | HIGH: аппаратный mute UDA1334A; LOW: звук разрешён |
| GPIO10 | `I2S_DATA_PIN`            | выход I2S0 DOUT           | PCM-данные для UDA1334A                            |
| GPIO11 | `I2S_LRCK_PIN`            | выход I2S0 WS/LRCK        | выбор левого/правого канала                        |
| GPIO12 | `I2S_BCK_PIN`             | выход I2S0 BCLK           | тактирование I2S                                   |
| GPIO15 | `GPIO_BUTTON_VOLUME_DOWN` | вход с внутренним pull-up | LOW: громкость −2%; удержание включает повтор      |
| GPIO16 | `GPIO_BUTTON_VOLUME_UP`   | вход с внутренним pull-up | LOW: громкость +2%; удержание включает повтор      |
| GPIO17 | `GPIO_BUTTON_POWER`       | вход с внутренним pull-up | LOW: Normal/Standby                                |
| GPIO18 | `GPIO_SPEAKER_RELAY`      | цифровой выход            | HIGH: подключить акустический выход реле           |
| GPIO21 | `GPIO_DISPLAY_BACKLIGHT`  | выход LEDC PWM            | 25 кГц, 10 бит; 0 — выключено, 1023 — 100%         |

## Кнопки

Каждая кнопка подключается между своим GPIO и GND. Внешнее напряжение на
кнопочные входы подавать не нужно. Firmware использует:

- внутренний pull-up;
- опрос каждые 5 мс без GPIO ISR;
- debounce из четырёх отсчётов, то есть 20 мс;
- повтор только для VOL− и VOL+ после задержки 500 мс, затем каждые 100 мс.

События проходят через общий control ingress с источником
`CONTROL_SOURCE_GPIO`. Кнопки не вызывают player, Flash или сетевые функции
непосредственно.

## Силовые выходы

GPIO8 и GPIO18 являются только логическими управляющими сигналами. Усилитель и
обмотку реле нельзя питать от GPIO. Требуются внешние MOSFET-ключи, общий GND,
защитный диод реле и питание нагрузки.

После `power_control_init()` оба выхода принудительно устанавливаются в LOW.
При включении сначала активируется AMP_POWER, firmware ждёт 6 секунд и готовый
PCM, затем включает SPEAKER_RELAY. При выключении реле снимается раньше питания
усилителя. Это уменьшает щелчки и защищает акустический выход.

## STATUS_WLED

GPIO2 предназначен для внешней цепочки из пяти адресных LED:

1. PWR;
2. AMP;
3. SPK;
4. WIFI;
5. BUF.

Для питания LED от 5 В схема предусматривает 74AHCT1G125, последовательный
резистор 330 Ом, общий GND и конденсатор 1000 мкФ по питанию. GPIO2 передаёт
только данные и не питает светодиоды.

Firmware не определяет автоматически GPIO встроенного WS2812 на DevKit.
В зависимости от производителя встроенный LED может находиться, например, на
GPIO48 или другом выводе. Такой LED не является `STATUS_WLED`, пока схема и
`GPIO_STATUS_WLED` явно не приведены к одному GPIO.

GPIO21 в этой прошивке занят PWM подсветки. Если встроенный WS2812 конкретной
платы физически подключён к GPIO21, PWM может оставить его в случайном цвете.
Подключать адресный LED к GPIO21 при текущей карте нельзя.

## Аудиовыход

Используется Philips I2S без MCLK:

- GPIO12 → BCLK;
- GPIO11 → WS/LRCK;
- GPIO10 → DIN UDA1334A;
- GPIO9 → MUTE.

GPIO9 устанавливается в HIGH при инициализации, остановке и аварийном
завершении I2S. Для гарантированного mute до запуска firmware рекомендуется
внешняя подтяжка, соответствующая входу конкретного модуля ЦАП.

## Проверка на устройстве

Безопасные read-only проверки:

- `GET /api/power` показывает состояния AMP_POWER, SPEAKER_RELAY,
  backlight PWM и WLED;
- для WLED должны быть `ready=true`, растущий `transmissions` и
  `errors=0`;
- `GET /api/ir` должен возвращать `receiver.ready=true`;
- `GET /api/player/state` с растущим `audio.pcm_write_count` подтверждает
  работу I2S GPIO9–GPIO12.
